/// <reference types="vite/client" />

/**
 * App version injected at build time from git commit hash
 */
declare const __APP_VERSION__: string;

/**
 * Build timestamp injected at build time
 */
declare const __BUILD_TIME__: string;
